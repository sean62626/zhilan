# 智览 (ZhiLan) — 多 Agent AI 自动化研报/新闻摘要生成平台

> 架构文档 · 技术栈说明 · 2026-07-27 更新

---

## 1. 项目概述

**智览** 是一个基于 **LangGraph 多 Agent 工作流** + **多源数据采集** 的 AI 自动化研报与新闻摘要生成平台。从多源信息采集 → 文本预处理 → 三层去重 → 语义聚类 → RAG 深度研报 → 质量审核 → 日报组装 → 导出，全流程 AI 驱动。

### 1.1 目标用户

- 需要每日行业/主题简报的研究员、分析师
- 需要跟踪特定领域动态的决策者
- 需要结构化研报输出的内容团队

### 1.2 核心能力

| 能力 | 说明 |
|---|---|
| **多源采集** | NewsAPI + 自定义爬虫并行采集，支持中英文 |
| **三层去重** | URL 精确 → SimHash 近似 → Embedding 语义，逐层过滤 |
| **语义聚类** | BGE Embedding + HDBSCAN 自动发现主题，LLM 生成标签与重要性评分 |
| **Agentic RAG** | Query 改写 → BM25+kNN 双路混合召回 → BGE Reranker 精排 → 结构化研报 |
| **审核-重试闭环** | 四维度审核（事实/幻觉/平衡/完整），不通过自动回退重生成（最多 3 次） |
| **全流程降级** | OpenSearch → 本地 TF-IDF，BGE Embedding → SimHash，LLM → 模板规则回退 |
| **实时监控** | WebSocket 推送工作流进度 + HTTP 轮询兜底 + 自动重连 |

---

## 2. 技术栈

| 层级 | 技术 | 说明 |
|---|---|---|
| **工作流编排** | LangGraph 0.2 | StateGraph + 条件边 + 循环 + MemorySaver Checkpointing |
| **LLM 调用** | DeepSeek (deepseek-chat) | OpenAI 兼容 API，指数退避重试（最多 3 次），80s 超时 |
| **Embedding** | BAAI/bge-large-zh-v1.5 | 1024 维中文向量，sentence-transformers 本地加载 |
| **Reranker** | BAAI/bge-reranker-v2-m3 | Cross-Encoder 精排 |
| **混合检索** | OpenSearch 2.19 | BM25 (multi_match) + kNN 向量检索 → RRF 合并 |
| **缓存/去重** | Redis 7.4 | API 响应缓存 · URL 去重 · 任务状态 |
| **API 服务** | FastAPI 0.115 | REST API + WebSocket 实时推送 |
| **异步任务** | APScheduler 3.10 + Celery 5.4 | 定时采集/日报生成 + 异步队列（备用） |
| **前端** | Nuxt 3 + Vue 3 + Tailwind CSS | Composition API · Pinia 状态管理 · ECharts 可视化 |
| **数据采集** | httpx + BeautifulSoup + NewsAPI | 并行采集 · 超时隔离 · 错误隔离 |
| **数据库** | MySQL 8.0 + SQLAlchemy 2.0 | 用户配置持久化 · 支持 SQLite 开发模式 |
| **容器化** | Docker Compose | 8 个服务：Nginx + Nuxt + FastAPI + Celery×2 + Redis + OpenSearch + MySQL |

---

## 3. 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端展示层                              │
│       Nuxt 3 + Vue 3 + Tailwind CSS + ECharts            │
│  Dashboard │ 简报中心 │ 聚类分析 │ 实时监控 │ 系统配置       │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTP (Nginx 反向代理) / WebSocket
┌──────────────────────┴──────────────────────────────────┐
│                   API 网关层 (FastAPI)                     │
│      REST API (11 个路由模块) · WebSocket · 生命周期管理      │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────┴──────────────────────────────────┐
│              LangGraph 工作流编排层 (8 节点)                │
│                                                            │
│  Collect → Preprocess → Dedup → Cluster → Research ⇄ Review → Compose → Export  │
│                                                            │
│  ★ 审核不通过 → 自动返回 Research 重试（最多 3 次循环）       │
│  ★ 全部节点支持降级策略，单点故障不阻塞管道                    │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────┴──────────────────────────────────┐
│                  数据与基础设施层                           │
│   Redis 7.4 · OpenSearch 2.19 · MySQL 8.0 · 文件存储       │
└─────────────────────────────────────────────────────────┘
```

### 3.1 Docker 部署拓扑

```
┌──────────────────────────────────────────┐
│                Nginx :80                  │
│     / → frontend:3000  /api → backend:8000│
│     /ws → backend:8000 (WebSocket 透传)   │
└──────────────────┬───────────────────────┘
                   │
    ┌──────────────┼──────────────┐
    │              │              │
┌───▼────┐  ┌──────▼──────┐  ┌───▼──────────┐
│Nuxt 3  │  │  FastAPI    │  │ Celery Worker │
│:3000   │  │  :8000      │  │ + Beat        │
└────────┘  └──────┬──────┘  └───┬──────────┘
                   │              │
    ┌──────────────┼──────────────┼──────────┐
    │              │              │          │
┌───▼────┐  ┌──────▼──────┐  ┌───▼────┐  ┌──▼───┐
│ Redis  │  │ OpenSearch  │  │ MySQL  │  │文件存储│
│ :6379  │  │   :9200     │  │ :3306  │  │exports│
└────────┘  └─────────────┘  └────────┘  └──────┘
```

---

## 4. LangGraph 工作流详细设计

### 4.1 工作流全景

```
                    ┌──────────┐
                    │  START   │
                    └────┬─────┘
                         │
                    ┌────▼─────┐
                    │ Collect  │  ← NewsAPI + 爬虫并行采集
                    └────┬─────┘
                         │
                    ┌────▼─────┐  [有文章?] → 无 → END
                    │Preprocess│  ← HTML 清洗 · 语言检测 · 分段
                    └────┬─────┘
                         │
                    ┌────▼─────┐
                    │  Dedup   │  ← URL 哈希 → SimHash → Embedding 语义
                    └────┬─────┘
                         │
                    ┌────▼─────┐  [有文章?] → 无 → END
                    │ Cluster  │  ← BGE Embedding + HDBSCAN + LLM 标签
                    └────┬─────┘
                         │
                    ┌────▼─────┐
                    │ Research │  ← Query 改写 → 混合检索 → Rerank → 研报生成
                    └────┬─────┘
                         │         ↑
                    ┌────▼─────┐   │ 不通过 (retry < 3)
                    │  Review  │───┘
                    └────┬─────┘
                         │ 通过 / retry >= 3
                    ┌────▼─────┐
                    │ Compose  │  ← LLM 日报组装 / 模板回退
                    └────┬─────┘
                         │
                    ┌────▼─────┐
                    │  Export  │  ← Markdown 导出 (weasyprint PDF 备用)
                    └────┬─────┘
                         │
                    ┌────▼─────┐
                    │   END    │
                    └──────────┘
```

### 4.2 8 节点职责矩阵

| # | 节点 | 核心职责 | 输入 | 输出 | 降级策略 |
|---|---|---|---|---|---|
| 1 | **Collect** | 并行多源采集 + 错误隔离 | 主题关键词、数据源配置 | RawArticle[] | 单源失败不影响其他源 |
| 2 | **Preprocess** | HTML 清洗 · 语言检测 | RawArticle[] | CleanArticle[] | — |
| 3 | **Dedup** | URL 精确 → SimHash → Embedding 三层去重 | CleanArticle[] | UniqueArticle[] | — |
| 4 | **Cluster** | Embedding + HDBSCAN + LLM 标签 | UniqueArticle[] | TopicCluster[] | TF-IDF 关键词替代 LLM 标签 |
| 5 | **Research** | Query 改写 → 双路召回 → Rerank → 研报 | TopicCluster[] + Corpus | ResearchReport[] | 检索降级 · 空研报占位 |
| 6 | **Review** | 四维度审核（事实/幻觉/平衡/完整） | ResearchReport[] + Corpus | ReviewResult[] | 审核异常强制通过 |
| 7 | **Compose** | LLM 组装每日简报 | ResearchReport[] + Review[] | DailyBrief | 模板规则回退 |
| 8 | **Export** | Markdown 导出 | DailyBrief | 文件路径 | — |

### 4.3 条件边与循环逻辑

审核-重试闭环是工作流的核心质量保障机制：

```python
# 审核路由
def review_router(state):
    if review_passed:
        return "compose"        # 全部通过 → 组装日报
    if retry_count >= 3:
        return "compose"        # 达上限 → 强制通过（附带审核意见）
    return "research"           # 不通过 → 回退 Research 重试

# 采集路由
def should_continue_after_collect(state):
    if raw_articles:
        return "preprocess"     # 有数据 → 继续
    return "end"                # 无数据 → 终止

# 去重路由
def should_continue_after_dedup(state):
    if unique_articles:
        return "cluster"        # 有数据 → 继续
    return "end"                # 全部重复 → 终止
```

**重试模式特点**：Research 节点在重试时仅重新生成未通过审核的簇（而非全部重来），同时将审核反馈（事实错误、幻觉问题、修改建议）注入 LLM 提示以改进生成质量。

### 4.4 State 设计

```python
class PlatformState(TypedDict):
    # 采集阶段
    raw_articles: list[dict]           # 原始文章
    collection_errors: list[str]       # 采集错误日志

    # 预处理阶段
    clean_articles: list[dict]         # 清洗后文章

    # 去重阶段
    unique_articles: list[dict]        # 去重后文章
    dedup_stats: dict                  # 去重统计

    # 聚类阶段
    topic_clusters: list[dict] | None  # 主题簇（None = 未执行）

    # 研报生成阶段
    research_reports: list[dict] | None

    # 审核阶段
    review_results: list[dict] | None
    review_passed: bool
    retry_count: int                   # 0-3

    # 组装导出
    daily_brief: dict | None
    export_paths: list[str]

    # 全局控制
    errors: Annotated[list[str], operator.add]  # append-only
    target_date: str
    topics: list[str]                  # 扁平关键词
    topics_detail: list[dict]          # 结构化主题配置
    workflow_status: str               # running | completed | failed
    run_id: str
```

---

## 5. 各模块详细设计

### 5.1 多源信息采集

#### 采集源

| 采集源 | 实现 | 说明 |
|---|---|---|
| **NewsAPI** | `collectors/newsapi.py` — httpx 异步调用 | 中英文关键词搜索，48h 窗口，日配额 100 次（免费版） |
| **自定义爬虫** | `collectors/crawler.py` — httpx + BeautifulSoup | 证券时报、每经网等定向爬取，支持用户自定义目标 |
| **Mock** | `collectors/mock.py` | 开发调试用模拟数据 |

#### 采集策略

- **并行执行**：所有采集源 `asyncio.gather` 并行执行，单源失败不影响其他源
- **超时隔离**：每个采集器独立超时（默认 90s），防止单源阻塞全局
- **主题注入**：用户可在 Settings 页面配置监控主题，实时注入采集器
- **诊断报告**：空返回时附带具体原因（API key 未配置、配额耗尽、关键词无匹配等）
- **采集器注册机制**：`collectors/__init__.py` 维护 `COLLECTOR_REGISTRY`，新增采集源只需注册函数

### 5.2 三层去重

| 层级 | 算法 | 阈值 | 说明 |
|---|---|---|---|
| **L1 精确** | URL MD5 哈希 → 集合去重 | 100% 匹配 | O(n)，最快 |
| **L2 近似** | SimHash 64-bit 指纹 | 汉明距离 < 12（相似度 > 0.85）| 中文 3-gram + 英文 2-gram 混合分词 |
| **L3 语义** | BGE Embedding 余弦相似度 | 相似度 > 0.92 | 不同表述但语义相同 |

### 5.3 语义聚类

1. **向量化**：BAAI/bge-large-zh-v1.5 → 1024 维向量（不可用时降级为 SimHash）
2. **降维**（可选）：PCA 降至 128 维，加速 HDBSCAN
3. **聚类**：HDBSCAN（`min_cluster_size=3`，EOM 选择法），自动确定簇数量
4. **关键词提取**：TF-IDF（回退为词频统计）
5. **标签生成**：DeepSeek LLM 为每个簇生成主题标签 + 重要性评分 1-10（回退为线性映射）

### 5.4 Agentic RAG 深度研报

```
Query 改写 ──→ 双路混合召回 ──→ Rerank 精排 ──→ 上下文组装 ──→ 结构化研报
```

| 步骤 | 技术 | 说明 |
|---|---|---|
| **Query 改写** | DeepSeek LLM | 主题 → 4 个不同角度检索 Query（回退为关键词组合） |
| **BM25 召回** | OpenSearch multi_match | 标题权重×2，best_fields 策略（降级为本地 TF-IDF） |
| **kNN 召回** | BGE Embedding + Cosine | 语义相似度（降级为 SimHash 汉明距离） |
| **RRF 合并** | Reciprocal Rank Fusion (k=60) | 双路结果融合去重 |
| **Rerank** | BGE Cross-Encoder | 精排取 Top-8（降级为保持原序） |
| **上下文组装** | 截断 + 格式化 | 按 token 预算拼接，标注来源索引 |
| **研报生成** | DeepSeek LLM | 四段式结构，强制来源引用 |

**研报输出结构**：
```
📖 一、事件背景 — 起因/脉络/参与方/时间线
📊 二、现状分析 — 最新动态/关键数据/各方立场（标注来源编号）
📈 三、趋势研判 — 短期(1-3月)/中长期(6-12月)/关键变量
⚠️ 四、风险提示 — 政策/市场/信息置信度
```

**写作规则（内置 Prompt 强制约束）**：
- 每条事实性陈述必须标注来源编号 `[来源N]`
- 只能使用提供的材料，不得编造数据
- 信息不足时明确写「基于现有信息无法判断」
- 区分「事实陈述」和「分析判断」

### 5.5 质量审核

| 检查维度 | 方法 | 不通过条件 |
|---|---|---|
| **事实准确性** | 关键数据/人名/时间与原文比对 | 关键数据与原文不符 |
| **幻觉检测** | 声明-证据对齐检查 | 出现原文不存在的实体/数据 |
| **平衡性** | 多源交叉验证 | 仅引用单一来源且无标注 |
| **完整性** | 四段式结构检查 | 缺少必要章节 |

### 5.6 每日简报结构

```
🔴 今日要闻 TOP5        — 按重要性排序的一句话摘要
📝 深度研报摘要         — 核心发现 + 关键数据 + 主要结论
🏭 行业动态速览         — 按用户配置主题体系归类
📉 数据看板             — 文章总数/簇数/研报数/通过数
🔮 明日关注             — 预告事件与关注要点
```

**LLM 不可用时**：自动切换为模板规则组装，用簇数据直接填充各章节，保证产出不中断。

### 5.7 事件总线 (WebSocket)

```
LangGraph Node ──→ publish_event_safe() ──→ WorkflowEventBus ──→ WebSocket ──→ 前端
                      (fire-and-forget)        (asyncio.Queue)     (实时推送)
```

事件类型：`node_started`、`node_progress`（详细子步骤）、`node_complete`、`workflow_complete`、`workflow_cancelled`、`workflow_error`、`heartbeat`

前端 `useWorkflowStream` composable 自动重连（指数退避 1s→30s），WebSocket 不可用时 HTTP 轮询兜底（5s 间隔）。

---

## 6. 缓存与降级策略 (Redis)

| 缓存类型 | Key 格式 | TTL | 说明 |
|---|---|---|---|
| API 响应 | `api:{endpoint}:{hash}` | 30min | NewsAPI 等外部 API |
| 文章去重 | `seen:{url_hash}` | 7d | 已处理文章 URL 指纹 |
| Embedding | `emb:{content_hash}` | 30d | 文本向量，避免重复计算 |
| 聚类结果 | `cluster:{date}:{topic}` | 24h | 当日聚类结果 |
| 研报缓存 | `report:{date}:{id}` | 7d | 已生成研报 |
| 日报缓存 | `brief:{date}` | 30d | 历史日报 |

**全链路降级**：
```
每层都设计降级路径，确保单点故障不阻塞管道：

OpenSearch 不可用 → 本地 TF-IDF 关键词检索
BGE Embedding 不可用 → SimHash 汉明距离（聚类 + 语义去重 + kNN 检索全部降级）
BGE Reranker 不可用 → 保持原始检索排序
DeepSeek API 不可用 → 模板规则生成日报 + 关键词回退 Query
审核异常 → 强制通过（不阻塞管道）
单采集源失败 → 其他源继续，结果合并
```

---

## 7. API 接口设计

| 方法 | 路径 | 说明 |
|---|---|---|
| **状态** | | |
| GET | `/api/v1/status` | 系统健康检查 + 依赖连通性 |
| GET | `/health` | Nginx 代理探测端点 |
| **工作流** | | |
| POST | `/api/v1/workflow/run` | 启动 8 节点全流程 |
| GET | `/api/v1/workflow/status/{id}` | 查询进度 + LangGraph State |
| POST | `/api/v1/workflow/{id}/stop` | 协作式取消 + Task.cancel 兜底 |
| **采集** | | |
| POST | `/api/v1/collect` | 触发即时采集 |
| **Pipeline** | | |
| POST | `/api/v1/pipeline/run` | 运行完整管道（可跳过研报） |
| POST | `/api/v1/pipeline/research` | 仅 RAG 研报生成 |
| POST | `/api/v1/pipeline/preprocess` | 仅预处理 |
| POST | `/api/v1/pipeline/dedup` | 仅去重 |
| POST | `/api/v1/pipeline/cluster` | 仅聚类 |
| **主题** | | |
| GET/POST | `/api/v1/topics` | 管理监控主题 |
| DELETE | `/api/v1/topics/{id}` | 删除主题 |
| **简报** | | |
| GET | `/api/v1/briefs` | 历史简报列表 |
| GET | `/api/v1/briefs/{date}` | 获取指定日期简报 |
| GET | `/api/v1/briefs/{date}/pdf` | 下载 PDF |
| **研报** | | |
| GET | `/api/v1/reports` | 研报列表 |
| GET | `/api/v1/reports/{id}` | 单篇研报详情 |
| **聚类** | | |
| GET | `/api/v1/clusters` | 当日聚类结果 |
| **配置** | | |
| GET/PUT | `/api/v1/config` | 系统配置 |
| **任务** | | |
| GET | `/api/v1/jobs` | 定时任务列表 + 历史 |
| **爬取目标** | | |
| GET/POST | `/api/v1/crawl-targets` | 管理自定义爬取目标 |
| DELETE | `/api/v1/crawl-targets/{id}` | 删除目标 |
| POST | `/api/v1/crawl-targets/{id}/restore` | 恢复已删除目标 |
| **实时推送** | | |
| WS | `/ws/v1/workflow/{run_id}` | 工作流实时进度 |

---

## 8. 前端架构

### 8.1 页面路由

| 页面 | 路由 | 核心内容 |
|---|---|---|
| **Dashboard** | `/` | 统计卡片 · 工作流控制 · 要闻 TOP5 · 最新研报 · 系统依赖 |
| **每日简报** | `/briefs` → `/briefs/:date` | 日期导航 · 完整简报 · PDF 下载 |
| **聚类分析** | `/clusters` | 主题簇列表 · 文章分组 |
| **实时监控** | `/monitor` | 工作流管道视图（8 节点） · Agent 状态 · 实时进度 |
| **系统配置** | `/settings` | 监控主题管理 · 爬取目标 · 数据源配置 · 定时任务 |

### 8.2 组件树

```
app.vue
└── layouts/default.vue
    ├── AppSidebar.vue          — 侧边栏导航
    │   └── NavItem.vue         — 导航项
    └── <NuxtPage>
        ├── pages/index.vue     — Dashboard
        │   ├── StatCard.vue    — 统计卡片
        │   ├── WorkflowPipeline.vue — 工作流管道（8 节点进度）
        │   ├── NewsItem.vue    — 要闻条目
        │   ├── ReportCard.vue  — 研报卡片
        │   ├── StatusBadge.vue — 状态标签
        │   └── EmptyState.vue  — 空状态占位
        ├── pages/briefs/*.vue  — 简报页
        ├── pages/reports/*.vue — 研报详情页
        ├── pages/clusters.vue  — 聚类分析页
        ├── pages/monitor.vue   — 实时监控页
        └── pages/settings.vue  — 系统配置页
```

### 8.3 数据流

```
用户操作 → Pinia Store (useWorkflowStore)
              │
              ├─→ useApi() → $fetch → Nuxt BFF (/api/v1/*) → Nginx → FastAPI
              │
              └─→ useWorkflowStream() → WebSocket → FastAPI EventBus
                     │
                     └─→ handleWsEvent() → Store 更新 → Vue 响应式 → UI
```

### 8.4 响应式断点

| 断点 | 宽度 | 布局 |
|---|---|---|
| Desktop | ≥ 1440px | 完整侧边栏 + 多列 |
| Laptop | 1024-1439px | 可折叠侧边栏 + 双列 |
| Tablet | 768-1023px | 抽屉式侧边栏 + 单列 |
| Mobile | < 768px | 底部导航 + 卡片流 |

---

## 9. 项目目录结构

```
aiagent2/
├── backend/                          # Python 后端
│   ├── app/
│   │   ├── main.py                   # FastAPI 入口（lifespan 管理 APScheduler）
│   │   ├── config.py                 # Pydantic Settings（LLM/Embedding/OS/Redis/MySQL）
│   │   ├── agents/                   # Agent 实现
│   │   │   ├── collector.py          # 多源采集编排（并行+超时+错误隔离）
│   │   │   ├── preprocessor.py       # 文本清洗 Agent
│   │   │   ├── dedup.py              # 三层去重 Agent
│   │   │   ├── cluster.py            # 语义聚类 Agent
│   │   │   ├── researcher.py         # RAG 研报生成 Agent
│   │   │   ├── reviewer.py           # 四维度审核 Agent
│   │   │   ├── composer.py           # 日报组装 Agent
│   │   │   └── exporter.py           # Markdown 导出 Agent
│   │   ├── collectors/               # 数据采集器
│   │   │   ├── __init__.py           # COLLECTOR_REGISTRY 注册表
│   │   │   ├── newsapi.py            # NewsAPI 采集（httpx 异步）
│   │   │   ├── crawler.py            # 自定义爬虫（httpx + BeautifulSoup）
│   │   │   └── mock.py               # 开发用模拟数据
│   │   ├── processors/               # 文本处理
│   │   │   ├── cleaner.py            # HTML 清洗
│   │   │   ├── segmenter.py          # 文本分段
│   │   │   ├── simhash.py            # SimHash 64-bit 指纹 + 汉明距离去重
│   │   │   └── clusterer.py          # HDBSCAN 聚类 + TF-IDF 关键词提取
│   │   ├── rag/                      # RAG 检索增强生成
│   │   │   ├── embedding.py          # BGE 模型加载（延迟单例）+ encode
│   │   │   ├── retriever.py          # 混合检索（BM25 + kNN → RRF 合并）
│   │   │   ├── reranker.py           # BGE Cross-Encoder 精排
│   │   │   ├── context.py            # 上下文组装 + 引用格式化
│   │   │   └── es_indexer.py         # OpenSearch 索引管理
│   │   ├── generators/               # LLM 生成
│   │   │   ├── summarizer.py         # DeepSeek API 调用封装（含指数退避重试）
│   │   │   ├── report_writer.py      # 研报生成 Prompt + 结构化解析
│   │   │   └── prompts.py            # 全部 Prompt 模板集中管理
│   │   ├── workflow/                 # LangGraph 工作流
│   │   │   ├── graph.py              # StateGraph 构建（8 节点 + 条件边）
│   │   │   ├── state.py              # PlatformState TypedDict 定义
│   │   │   ├── nodes.py              # 8 个 Node 函数实现
│   │   │   ├── streaming.py          # 异步执行 + 事件发布 + 取消机制
│   │   │   └── event_bus.py          # asyncio.Queue Pub/Sub 事件总线
│   │   ├── api/                      # API 层
│   │   │   ├── routes/
│   │   │   │   ├── health.py         # 健康检查
│   │   │   │   ├── collect.py        # 采集触发
│   │   │   │   ├── pipeline.py       # Pipeline 分步执行
│   │   │   │   ├── workflow.py       # 工作流启动/状态/停止
│   │   │   │   ├── topics.py         # 主题 CRUD
│   │   │   │   ├── briefs.py         # 简报查询/PDF
│   │   │   │   ├── reports.py        # 研报查询
│   │   │   │   ├── clusters.py       # 聚类查询
│   │   │   │   ├── config.py         # 系统配置
│   │   │   │   ├── jobs.py           # 定时任务管理
│   │   │   │   └── crawl_targets.py  # 爬取目标管理
│   │   │   └── websocket.py          # WebSocket 端点
│   │   ├── models/                   # 数据模型
│   │   │   ├── base.py               # SQLAlchemy Base + Session
│   │   │   ├── article.py            # RawArticle / CleanArticle / TopicCluster / ResearchReport
│   │   │   └── report.py             # ReviewResult / DailyBrief
│   │   ├── scheduler/                # 任务调度
│   │   │   ├── __init__.py           # APScheduler 启动/关闭
│   │   │   ├── jobs.py               # 3 个定时任务定义
│   │   │   ├── history.py            # 任务执行历史追踪
│   │   │   ├── celery_app.py         # Celery 实例
│   │   │   └── worker.py             # Celery Worker 配置
│   │   └── cache/                    # Redis 缓存
│   │       └── cache_manager.py      # 缓存管理器
│   ├── configs/                      # YAML 配置
│   │   ├── sources.yaml              # 数据源配置（NewsAPI + 爬虫）
│   │   ├── settings.yaml             # 全局设置
│   │   └── topics.json               # 用户监控主题持久化
│   ├── exports/                      # 导出文件输出目录
│   ├── tests/                        # 测试
│   ├── requirements.txt
│   └── Dockerfile                    # Python 3.12-slim + CPU PyTorch
│
├── frontend/                         # Nuxt 3 前端
│   ├── pages/                        # 文件系统路由（5 个页面）
│   ├── layouts/default.vue           # 默认布局（侧边栏 + 内容区）
│   ├── components/                   # 8 个 Vue 组件
│   ├── composables/                  # useApi / useWorkflowStream
│   ├── stores/workflow.ts            # Pinia 工作流状态管理
│   ├── plugins/pinia.ts              # Pinia 插件注册
│   ├── server/api/                   # Nuxt BFF 代理层
│   ├── types/index.ts                # TypeScript 类型定义
│   ├── assets/css/main.css           # 全局样式
│   ├── nuxt.config.ts                # Nuxt 配置
│   ├── tailwind.config.ts            # Tailwind 配置
│   ├── package.json
│   └── Dockerfile                    # Node 20-alpine
│
├── nginx/nginx.conf                  # Nginx 反向代理（前端 + API + WebSocket）
├── hf_models/                        # HuggingFace 模型缓存（BGE Embedding/Reranker）
├── docker-compose.yaml               # 8 服务全栈编排
├── .env.example                      # 环境变量模板
├── 项目介绍.md                        # 本文件
└── 使用指南.md                        # 使用指南
```

---

## 10. 关键设计决策

### 10.1 为什么用 OpenSearch 而不是 Elasticsearch？

OpenSearch 是 ES 7.x 的 Apache 2.0 分支，API 完全兼容。选择理由：
- 完全开源，无 License 风险
- 与 ES 客户端库（opensearch-py）兼容
- 2.19 版本稳定，kNN + BM25 混合检索完全满足需求
- Docker 单节点模式开箱即用（禁用安全插件）

### 10.2 为什么用 APScheduler 而不是纯 Celery？

- APScheduler 集成在 FastAPI 进程内，部署更简单
- 任务数量有限（3 个定时任务），不需要独立的 Worker 集群
- Celery 保留在 docker-compose 中作为异步研报生成的备用方案
- APScheduler 的 `AsyncIOScheduler` 与 FastAPI 的 async 生态完美契合

### 10.3 全链路降级设计

项目核心理念是「**管道不应因单点故障而中断**」。每一层都有降级路径：

| 层级 | 主方案 | 降级方案 |
|---|---|---|
| 检索 | OpenSearch BM25 | 本地 scikit-learn TF-IDF |
| 向量化 | BGE Embedding | SimHash 汉明距离 |
| 精排 | BGE Reranker | 保持原始检索排序 |
| 日报 | DeepSeek LLM | 模板规则组装 |
| Query 改写 | DeepSeek LLM | 关键词直接拼接 |
| 聚类标签 | DeepSeek LLM | TF-IDF 关键词 + 线性重要性评分 |
| 审核 | DeepSeek LLM | 异常时强制通过 |
| 采集 | 全部源并行 | 单源失败不影响其他 |

### 10.4 审核-重试闭环

这是平台区别于简单 RAG 摘要的核心设计：
1. ResearchAgent 生成研报后，ReviewAgent 进行四维度审核
2. 不通过 → 提取审核反馈（具体错误和建议）
3. 回退 ResearchAgent，仅重新生成失败的簇（保留已通过的）
4. LLM 收到审核反馈作为修正指引
5. 最多 3 次循环，超过则强制通过（附带审核意见标记）
6. 审核异常直接通过，不阻塞管道

---

## 11. 定时任务

| 任务 | 调度 | 说明 |
|---|---|---|
| `collect_all_sources` | 可配置（默认每 2 小时） | 从所有数据源采集新闻并索引到 OpenSearch |
| `generate_daily_brief` | 可配置（默认 08:00） | 触发完整 8 节点工作流生成日报 |
| `health_check` | 每 5 分钟 | 检查 Redis/OpenSearch/MySQL 连通性 |

所有定时任务通过 `history_tracker` 记录执行历史（成功/失败/耗时/摘要），前端 Settings 页面可视化展示。

---

## 12. 环境变量

```env
# LLM (DeepSeek)
DEEPSEEK_API_KEY=sk-xxx
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# Embedding
EMBEDDING_MODEL=BAAI/bge-large-zh-v1.5
EMBEDDING_DEVICE=cpu

# Reranker
RERANKER_MODEL=BAAI/bge-reranker-v2-m3

# OpenSearch (原 ES_HOST 已重命名为 OS_HOST)
OS_HOST=http://opensearch:9200
OS_INDEX=news_articles

# Redis
REDIS_URL=redis://redis:6379/0

# MySQL
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_USER=zhilan
MYSQL_PASSWORD=zhilan_secret
MYSQL_DATABASE=zhilan

# 数据采集
NEWSAPI_KEY=xxx
COLLECTION_INTERVAL_HOURS=2
BRIEF_GENERATION_TIME=08:00

# 前端 BFF
BACKEND_URL=http://localhost:8000

# 应用
APP_ENV=development
LOG_LEVEL=INFO
DEBUG=true
SECRET_KEY=dev-secret-key-change-in-production
```

---

## 13. 快速启动

```bash
# 1. 克隆项目
git clone <repo-url> && cd aiagent2

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env 填入 DEEPSEEK_API_KEY 和 NEWSAPI_KEY

# 3. 启动全栈服务
docker-compose up -d

# 4. 访问
# 前端: http://localhost
# API 文档: http://localhost/api/docs
```

---

## 14. 参考资源

- LangGraph: https://langchain-ai.github.io/langgraph/
- DeepSeek API: https://platform.deepseek.com/api-docs
- BGE 模型: https://huggingface.co/BAAI/bge-large-zh-v1.5
- Nuxt 3: https://nuxt.com/docs
- OpenSearch: https://opensearch.org/docs/
- HDBSCAN: https://hdbscan.readthedocs.io/

---

> **本文件为项目架构文档，所有开发决策应参照此文档。如有架构变更需求，先更新本文档再执行。**
