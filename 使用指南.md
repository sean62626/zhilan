# 智览 — AI 自动化研报/新闻摘要生成平台 · 使用指南

> 基于 LangGraph 多 Agent 工作流 + 定向爬虫 + RAG 检索增强的全流程 AI 驱动研报平台

---

## 目录

1. [平台简介](#1-平台简介)
2. [快速启动](#2-快速启动)
3. [服务与端口](#3-服务与端口)
4. [前端页面导航](#4-前端页面导航)
5. [API 使用指南](#5-api-使用指南)
   - [5.1 健康检查](#51-健康检查)
   - [5.2 数据采集](#52-数据采集)
   - [5.3 处理管道（Pipeline）](#53-处理管道pipeline)
   - [5.4 工作流管理](#54-工作流管理)
   - [5.5 主题管理](#55-主题管理)
   - [5.6 定时任务管理](#56-定时任务管理)
   - [5.7 WebSocket 实时推送](#57-websocket-实时推送)
   - [5.8 系统配置](#58-系统配置)
6. [Agent 工作流详解](#6-agent-工作流详解)
7. [环境变量配置](#7-环境变量配置)
8. [技术架构速览](#8-技术架构速览)
9. [常见问题](#9-常见问题)

---

## 1. 平台简介

智览是一个全流程 AI 驱动的新闻研报生成平台，通过 **9 个专用 Agent** 协作，将多源信息自动转化为结构化日报/研报。

### 核心能力

| 环节 | 能力 | 技术实现 |
|---|---|---|
| **多源采集** | NewsAPI / RSS / 爬虫 / PDF 解析 | CollectorAgent 并行采集，单源失败不影响其他 |
| **智能去重** | URL 精确 → SimHash 近似 → Embedding 语义 | 三层递进去重，过滤率 > 30% |
| **语义聚类** | BGE 向量化 + HDBSCAN | 自动发现热点话题，LLM 生成标签 |
| **RAG 深度研报** | Query 改写 → 多路召回 → Rerank → 生成 | 混合检索（BM25 + kNN）+ DeepSeek V4 Pro |
| **质量审核** | 事实核查 + 幻觉检测 + 平衡性检查 | ReviewAgent 不通过自动重试（最多 3 次） |
| **多格式导出** | Markdown / PDF | ExportAgent 一键导出 |
| **定时调度** | 自动采集 + 日报生成 | APScheduler + Celery Beat 双重保障 |

---

## 2. 快速启动

### 前提条件

- **Docker Desktop** 已安装并运行
- 推荐系统内存 ≥ 8GB（Elasticsearch 需要 512MB+）
- 磁盘空间 ≥ 15GB（含 PyTorch 模型镜像）

### 一键启动

```bash
# 进入项目目录
cd D:\aiagent2

# 首次拉取基础镜像并启动所有服务（首次需构建，约 10-15 分钟）
docker compose up -d --build

# 日常启动服务
docker compose up -d

# 查看服务状态
docker compose ps
```

### 停止与重启

```bash
# 停止所有服务
docker compose down

# 更改代码后重启后端
docker-compose restart backend

# 重启单个服务（代码改动后热重载，通常无需手动重启）
docker compose restart backend
docker compose restart frontend

# 完全清除（包括数据卷）
docker compose down -v
```

### 启动后验证

浏览器访问以下地址确认各服务正常：

| 地址 | 内容 |
|---|---|
| `http://localhost` | 前端页面（Dashboard） |
| `http://localhost:8000/health` | 后端健康检查 |
| `http://localhost:8000/api/v1/status` | 综合运行状态（依赖检查） |
| `http://localhost:8000/api/docs` | Swagger API 文档 |

---

## 3. 服务与端口

| 服务 | 容器名 | 端口（宿主机） | 内部端口 | 说明 |
|---|---|---|---|---|
| **Nginx** | `zhilan-nginx` | `80` | `80` | 反向代理，统一入口 |
| **前端 Nuxt 3** | `zhilan-frontend` | `3000` | `3000` | SSR + BFF 代理层 |
| **后端 FastAPI** | `zhilan-backend` | `8000` | `8000` | 热重载，改动即生效 |
| **Celery Worker** | `zhilan-celery-worker` | — | — | 异步研报生成 |
| **Celery Beat** | `zhilan-celery-beat` | — | — | 定时任务调度 |
| **OpenSearch 2.19** | `zhilan-opensearch` | `9201` | `9200` | 混合检索（kNN + BM25） |
| **Redis 7.4** | `zhilan-redis` | `6379` | `6379` | 缓存 / 去重 / 消息队列 |
| **MySQL 8.0** | `zhilan-mysql` | `3306` | `3306` | 用户配置 / 研报记录 |

### 查看日志

```bash
# 查看全部服务日志
docker compose logs -f

# 查看指定服务日志（最近 50 行，实时跟踪）
docker compose logs -f backend --tail 50
docker compose logs -f frontend --tail 50
docker compose logs -f celery-worker --tail 50
```

---

## 4. 前端页面导航

| 页面 | 路由 | 功能 |
|---|---|---|
| **Dashboard 工作台** | `/` | 统计卡片、今日要闻 TOP5、采集趋势图、最新研报、工作流实时状态 |
| **每日简报** | `/briefs` | 日期导航、要闻排行、研报卡片、行业动态、数据看板 |
| **研报详情** | `/reports/:id` | 结构化四段式研报、引用溯源标注、关键数据表格、PDF 导出 |
| **聚类分析** | `/clusters` | 力导向关系图（ECharts）、主题簇列表、事件时间线 |
| **实时监控** | `/monitor` | 工作流管道视图、Agent 状态卡片、实时日志流 |
| **系统配置** | `/settings` | 监控主题管理、数据源配置、采集频率、推送渠道 |

---

## 5. API 使用指南

### 接口总览

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/v1/status` | 综合运行状态 |
| `GET` | `/api/v1/ready` | K8s 就绪探针 |
| `POST` | `/api/v1/collect` | 触发即时采集 |
| `GET` | `/api/v1/collect/status` | 最近采集状态 |
| `POST` | `/api/v1/pipeline/run` | 运行完整处理管道 |
| `POST` | `/api/v1/pipeline/preprocess` | 仅预处理 |
| `POST` | `/api/v1/pipeline/dedup` | 仅去重 |
| `POST` | `/api/v1/pipeline/cluster` | 仅聚类 |
| `POST` | `/api/v1/pipeline/research` | 仅 RAG 研报 |
| `POST` | `/api/v1/workflow/run` | 启动 LangGraph 工作流 |
| `GET` | `/api/v1/workflow/status/{run_id}` | 查询工作流状态 |
| `GET` | `/api/v1/topics` | 获取监控主题 |
| `POST` | `/api/v1/topics` | 新增监控主题 |
| `DELETE` | `/api/v1/topics/{id}` | 删除监控主题 |
| `GET` | `/api/v1/jobs` | 列出定时任务 |
| `GET` | `/api/v1/jobs/history` | 任务执行历史 |
| `POST` | `/api/v1/jobs/{id}/trigger` | 手动触发任务 |
| `POST` | `/api/v1/jobs/{id}/enable` | 启用任务 |
| `POST` | `/api/v1/jobs/{id}/disable` | 禁用任务 |
| `GET` | `/api/v1/config` | 获取系统配置 |
| `PUT` | `/api/v1/config` | 更新系统配置 |
| `GET` | `/api/v1/briefs` | 历史日报列表 |
| `GET` | `/api/v1/briefs/{date}` | 指定日期日报 |
| `GET` | `/api/v1/reports` | 研报列表 |
| `GET` | `/api/v1/reports/{id}` | 单篇研报详情 |
| `GET` | `/api/v1/clusters` | 当日聚类结果 |
| `WS` | `/ws/v1/workflow/{run_id}` | 工作流实时推送 |

---

### 5.1 健康检查

```bash
# 根路径健康检查（Nginx 代理探测用）
curl http://localhost:8000/health
# → {"status":"ok","service":"zhilan-backend"}

# 综合运行状态（依赖检查并行，1.5s 超时）
curl http://localhost:8000/api/v1/status
# → {
#   "service": "zhilan-backend",
#   "version": "0.1.0",
#   "healthy": true,
#   "dependencies": {
#     "opensearch": {"status": "connected", "version": "2.19.0"},
#     "redis": {"status": "connected"},
#     "mysql": {"status": "connected"}
#   },
#   "scheduler": {"status": "running", "jobs_count": 3}
# }

# 就绪探针（轻量，仅连通性）
curl http://localhost:8000/api/v1/ready
```

---

### 5.2 数据采集

```bash
# 触发即时采集（从所有已配置数据源）
curl -X POST http://localhost:8000/api/v1/collect
# → {
#   "status": "completed",
#   "total_sources": 3,
#   "successful_sources": 3,
#   "total_articles": 45,
#   "indexed_articles": 45,
#   "source_stats": {
#     "newsapi": 20,
#     "rss": 15,
#     "crawler": 10
#   },
#   "elapsed_seconds": 3.2
# }

# 查询最近一次采集状态
curl http://localhost:8000/api/v1/collect/status
```

采集数据会自动写入 OpenSearch 索引，供后续 RAG 检索使用。

---

### 5.3 处理管道（Pipeline）

处理管道按顺序执行：采集 → 预处理 → 去重 → 聚类 → 研报生成。可以全量运行或分步调试：

```bash
# ===== 全量运行（采集到研报） =====
curl -X POST "http://localhost:8000/api/v1/pipeline/run?skip_research=false"
# 跳过研报阶段（只做到聚类）：
curl -X POST "http://localhost:8000/api/v1/pipeline/run?skip_research=true"

# ===== 分步调试 =====

# 仅预处理（传入原始文章列表）
curl -X POST http://localhost:8000/api/v1/pipeline/preprocess \
  -H "Content-Type: application/json" \
  -d '[{"id":"1","title":"测试文章","url":"http://example.com","content":"正文内容","source":"rss","source_name":"36氪","language":"zh"}]'

# 仅去重
curl -X POST http://localhost:8000/api/v1/pipeline/dedup \
  -H "Content-Type: application/json" \
  -d '[{"id":"1","title":"...","url":"...","cleaned_content":"...","source":"rss"}]'

# 仅聚类
curl -X POST http://localhost:8000/api/v1/pipeline/cluster \
  -H "Content-Type: application/json" \
  -d '[...]'

# 仅 RAG 研报（对已有聚类结果生成研报）
curl -X POST "http://localhost:8000/api/v1/pipeline/research?max_reports=3" \
  -H "Content-Type: application/json" \
  -d '{"clusters": [...], "articles": [...]}'
```

---

### 5.4 工作流管理

LangGraph 工作流以异步后台任务方式执行，支持状态查询和 WebSocket 实时推送：

```bash
# 启动工作流（支持指定多个监控主题）
curl -X POST "http://localhost:8000/api/v1/workflow/run?topics=AI&topics=芯片&topics=大模型"
# → {"status":"ok","message":"工作流已启动","run_id":"a1b2c3d4e5f6","topics":["AI","芯片","大模型"]}

# 查询工作流运行状态
curl http://localhost:8000/api/v1/workflow/status/a1b2c3d4e5f6
# → {
#   "run_id": "a1b2c3d4e5f6",
#   "status": "running",
#   "nodes_completed": ["collect", "preprocess", "dedup"],
#   "stats": {
#     "raw_articles": 100,
#     "clean_articles": 95,
#     "unique_articles": 72,
#     "topic_clusters": 0,
#     "review_passed": false,
#     "retry_count": 0
#   },
#   "errors": []
# }
```

工作流包含 8 个节点：`collect → preprocess → dedup → cluster → research → review → compose → export`，其中审核不通过会自动回退到 research 重试（最多 3 次）。

---

### 5.5 主题管理

```bash
# 获取所有监控主题
curl http://localhost:8000/api/v1/topics
# → 默认包含 科技、金融、国际 三个主题

# 新增监控主题
curl -X POST http://localhost:8000/api/v1/topics \
  -H "Content-Type: application/json" \
  -d '{"name":"新能源","keywords":["光伏","风电","储能","锂电池","氢能"]}'

# 删除主题
curl -X DELETE http://localhost:8000/api/v1/topics/a1b2c3d4
```

主题配置文件存储在 `backend/configs/topics.json`，新增/删除实时生效。

---

### 5.6 定时任务管理

系统内置 3 个定时任务：

| 任务 ID | 名称 | 默认频率 | 说明 |
|---|---|---|---|
| `collect_all_sources` | 定时采集 | 每 2 小时 | 从所有数据源采集新闻 |
| `generate_daily_brief` | 日报生成 | 每天 08:00 | 触发完整 LangGraph 工作流 |
| `health_check` | 健康检查 | 每 5 分钟 | 验证 Redis/OpenSearch/MySQL 连通性 |

```bash
# 列出所有定时任务
curl http://localhost:8000/api/v1/jobs

# 查询执行历史
curl "http://localhost:8000/api/v1/jobs/history?job_id=collect_all_sources&limit=10"

# 手动立即触发一次采集
curl -X POST http://localhost:8000/api/v1/jobs/collect_all_sources/trigger

# 禁用/启用定时任务
curl -X POST http://localhost:8000/api/v1/jobs/collect_all_sources/disable
curl -X POST http://localhost:8000/api/v1/jobs/collect_all_sources/enable
```

---

### 5.7 WebSocket 实时推送

订阅指定工作流的实时进度事件：

```javascript
// 前端示例
const runId = "a1b2c3d4e5f6";
const ws = new WebSocket(`ws://localhost:8000/ws/v1/workflow/${runId}`);

ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  switch (data.type) {
    case "connected":
      console.log("WebSocket 已连接");
      break;
    case "node_complete":
      console.log(`节点完成: ${data.node}`, data);
      break;
    case "workflow_complete":
      console.log("工作流完成!", data.state_summary);
      break;
    case "workflow_error":
      console.error("工作流出错:", data.error);
      break;
    case "heartbeat":
      // 30 秒心跳，保持连接
      break;
  }
};
```

---

### 5.8 系统配置

```bash
# 获取当前配置
curl http://localhost:8000/api/v1/config
# → {
#   "status": "ok",
#   "config": {
#     "app_name": "智览",
#     "app_version": "0.1.0",
#     "environment": "development",
#     "llm_model": "deepseek-chat",
#     "llm_api_configured": true,
#     "collection_interval_hours": 2,
#     "brief_generation_time": "08:00",
#     "os_configured": true,
#     ...
#   }
# }

# 更新采集间隔和日报生成时间
curl -X PUT http://localhost:8000/api/v1/config \
  -H "Content-Type: application/json" \
  -d '{
    "collection_interval_hours": 4,
    "brief_generation_time": "09:30"
  }'
# 配置变更后调度规则自动重载，无需重启
```

---

## 6. Agent 工作流详解

### 6.1 工作流全景

```
Start → Collect → Preprocess → Dedup → Cluster → Research → Review → Compose → Export → End
                                                      ↑              │
                                                      └── 不通过 ────┘  (最多 3 次)
```

### 6.2 Agent 职责矩阵

| # | Agent | 职责 | 输入 | 输出 |
|---|---|---|---|---|
| 1 | **CollectorAgent** | 多源并行采集（NewsAPI/RSS/爬虫/PDF） | 主题关键词 + 数据源列表 | `RawArticle[]` |
| 2 | **PreprocessAgent** | HTML 清洗、语言检测、文本分段 | `RawArticle[]` | `CleanArticle[]` |
| 3 | **DedupAgent** | 三层去重（URL 精确 → SimHash 近似 → Embedding 语义） | `CleanArticle[]` | `UniqueArticle[]` |
| 4 | **ClusterAgent** | BGE 向量化 + HDBSCAN 聚类 + LLM 标签生成 | `UniqueArticle[]` | `TopicCluster[]` |
| 5 | **ResearchAgent** | RAG 检索增强 + DeepSeek 四段式研报生成 | `TopicCluster[]` + 检索上下文 | `ResearchReport[]` |
| 6 | **ReviewAgent** | 事实核查 + 幻觉检测 + 平衡性检查 | `ResearchReport[]` + 原文引用 | `ReviewResult[]` |
| 7 | **ComposeAgent** | 日报组装（TOP5 要闻 + 深度研报 + 行业速览 + 明日关注） | `ResearchReport[]` + `ReviewResult[]` | `DailyBrief` |
| 8 | **ExportAgent** | Markdown / PDF 导出 | `DailyBrief` | 文件路径 |
| 9 | **DispatchAgent** | REST API + 文件推送 | `DailyBrief` + 导出文件 | 推送结果 |

### 6.3 Quality Control 循环

```
Research → Review → 通过? → Compose
              │
              └── 不通过 → Research (retry_count + 1)
                              │
                              └── retry >= 3 → 强制通过（附审核意见）
```

### 6.4 研报输出结构

每篇研报遵循四段式结构化输出：

```
📖 一、事件背景
   - 事件起因与历史脉络
   - 关键参与方与利益关系

📊 二、现状分析
   - 最新动态与关键数据
   - 各方立场与反应

📈 三、趋势研判
   - 短期预测（1-3 个月）
   - 中长期展望（6-12 个月）

⚠️ 四、风险提示
   - 政策/监管风险
   - 市场波动风险
   - 信息来源与置信度标注
```

---

## 7. 环境变量配置

核心配置文件为 `backend/.env`（开发模式）和项目根目录 `.env`（Docker 模式）：

```env
# ========== LLM (DeepSeek V4 Pro) ==========
DEEPSEEK_API_KEY=sk-your-key-here          # ⚠️ 必须修改为真实 API Key
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# ========== Embedding ==========
EMBEDDING_MODEL=BAAI/bge-large-zh-v1.5     # 中文语义向量化
EMBEDDING_DEVICE=cpu                        # CPU / cuda

# ========== Reranker ==========
RERANKER_MODEL=BAAI/bge-reranker-v2-m3     # Cross-Encoder 精排

# ========== OpenSearch ==========
OS_HOST=http://opensearch:9200              # Docker 内网地址
OS_INDEX=news_articles                     # 索引名称

# ========== Redis ==========
REDIS_URL=redis://redis:6379/0

# ========== 数据库 ==========
# 开发模式（SQLite，无需 MySQL）：
DATABASE_URL=sqlite:///./zhilan.db
# Docker 模式（启用 MySQL）：
# MYSQL_HOST=mysql
# MYSQL_USER=zhilan
# MYSQL_PASSWORD=zhilan_secret
# MYSQL_DATABASE=zhilan

# ========== NewsAPI ==========
NEWSAPI_KEY=your-newsapi-key               # https://newsapi.org 注册获取

# ========== 调度 ==========
COLLECTION_INTERVAL_HOURS=2                 # 采集间隔
BRIEF_GENERATION_TIME=08:00                # 日报生成时间

# ========== 应用 ==========
APP_ENV=development
LOG_LEVEL=INFO
```

> **⚠️ 重要**：`DEEPSEEK_API_KEY` 必须配置为真实的 DeepSeek API Key，否则所有 LLM 功能（摘要、研报、聚类标签）无法工作。

---

## 8. 技术架构速览

```
┌─────────────────────────────────────────────────────────┐
│                  前端展示层（Nuxt 3 + Vue 3）              │
│   Dashboard │ 简报中心 │ 数据看板 │ 配置管理 │ 实时监控      │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTP / WebSocket
┌──────────────────────┴──────────────────────────────────┐
│              Nginx :80 → 前端 :3000 / 后端 :8000          │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────┴──────────────────────────────────┐
│                   FastAPI 后端 :8000                      │
│   LangGraph 工作流 (9 Agent)  │  Celery 异步任务         │
└──────┬──────────┬──────────┬──────────┬─────────────────┘
       │          │          │          │
┌──────▼──┐ ┌─────▼───┐ ┌───▼────┐ ┌──▼──────────┐
│OpenSearch│ │  Redis  │ │ MySQL  │ │ Celery Beat │
│ 2.19    │ │  7.4    │ │  8.0   │ │ + Worker    │
│kNN+BM25 │ │缓存/去重│ │ 配置/  │ │ 异步任务     │
│         │ │         │ │ 记录   │ │             │
└─────────┘ └─────────┘ └────────┘ └─────────────┘
```

---

## 9. 常见问题

### Q1：Docker 镜像拉取失败（EOF / timeout）

国内网络访问 Docker Hub 可能受限，解决方案：

1. Docker Desktop → Settings → Docker Engine，添加镜像加速器：
   ```json
   "registry-mirrors": [
     "https://docker.1panel.live",
     "https://hub.rat.dev",
     "https://docker.m.daocloud.io"
   ]
   ```
2. 点击 Apply & Restart 生效

### Q2：9200 端口被占用

`cpolar` 等内网穿透工具可能占用 9200 端口。OpenSearch 已映射为宿主机 `9201` 端口，容器内部仍使用 `9200`，不影响服务间通信。

### Q3：后端显示「API 无响应」

通常是前端 BFF 代理配置问题。确认 `frontend/server/api/v1/[...].ts` 中目标地址为：
```typescript
const target = `http://backend:8000${event.path}`;
```
容器内不能使用 `localhost` / `127.0.0.1`，必须用 Docker 服务名。

### Q4：研报生成没有内容 / LLM 调用失败

1. 确认 `DEEPSEEK_API_KEY` 已配置为真实有效的 API Key（非 `sk-xxx` 占位符）
2. 确认 `NEWSAPI_KEY` 已配置（如果使用 NewsAPI 数据源）
3. 查看后端日志排查：
   ```bash
   docker compose logs backend --tail 100
   ```

### Q5：首次启动构建时间很长

后端镜像包含 PyTorch（CPU 版）+ BGE Embedding 模型 + HDBSCAN + scikit-learn 等科学计算库，首次 `pip install` 需要编译 C 扩展，约 10-15 分钟。后续启动使用缓存，秒级完成。

### Q6：如何启用 MySQL 替代 SQLite

编辑 `backend/.env`：
```env
# 注释掉 SQLite
# DATABASE_URL=sqlite:///./zhilan.db
# 启用 MySQL（默认密码已配置在 docker-compose.yaml 中）
MYSQL_HOST=mysql
MYSQL_USER=zhilan
MYSQL_PASSWORD=zhilan_secret
MYSQL_DATABASE=zhilan
```

### Q7：如何单独启动/重启某个服务

```bash
docker compose up -d backend         # 启动后端
docker compose restart frontend      # 重启前端
docker compose logs -f celery-worker # 查看 Worker 日志
```

### Q8：修改代码后需要重新构建镜像吗

**不需要**。开发模式下前后端均启用了热重载（volume 挂载 + `--reload`）：
- 后端（Uvicorn `--reload`）：修改 Python 文件自动重启
- 前端（Nuxt dev）：修改 `.vue` / `.ts` 文件自动 HMR

---

> **📅 文档版本**：v1.0 · 2026-07-25  
> **🔗 项目仓库**：`D:\aiagent2`  
> **📖 架构文档**：`CLAUDE.md`
